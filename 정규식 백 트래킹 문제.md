특정한 정규식은 오랜 시간이 걸리게 된다.  특히 

```js
const emptySpaces = Array.from({ length: 50000 }).map((v) => " ").join("")
const str = emptySpaces.concat(',').concat(emptySpaces)
console.time('a')
str.concat(str).concat(str).split(/\s*,\s*/, 1)
console.timeEnd('a')
// VM1007:3 a: 0.435791015625 ms
```